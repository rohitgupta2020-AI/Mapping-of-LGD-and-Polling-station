# Copy this file to .streamlit/secrets.toml for local use.
# In Streamlit Community Cloud, paste the same content into App settings > Secrets.
#
# Generate a password hash:
#   python scripts/hash_password.py "your-password"

[auth.users.user1]
password_hash = "replace-with-sha256-hash"

[auth.users.user2]
password_hash = "replace-with-sha256-hash"

[auth.users.user3]
password_hash = "replace-with-sha256-hash"

[auth.users.user4]
password_hash = "replace-with-sha256-hash"

[auth.users.user5]
password_hash = "replace-with-sha256-hash"

[auth.users.user6]
password_hash = "replace-with-sha256-hash"

[auth.users.user7]
password_hash = "replace-with-sha256-hash"

[auth.users.user8]
password_hash = "replace-with-sha256-hash"

[auth.users.user9]
password_hash = "replace-with-sha256-hash"

[auth.users.user10]
password_hash = "replace-with-sha256-hash"
